# =============================================
# RAG Chatbot Dependencies
# =============================================

# --- LangChain (only the bits we use: core types + text splitter + Ollama integration) ---
langchain-core>=1.3.0
langchain-text-splitters>=0.3.0

# --- Embeddings ---
sentence-transformers==3.4.1

# --- Vector Store ---
chromadb>=0.5.0

# --- LLM Provider (local, free) ---
# 1.1.0+ handles "thinking" models (Qwen 3+, DeepSeek-R1) correctly via reasoning=False
langchain-ollama>=1.1.0

# --- Document Loaders ---
pypdf==5.4.0

# --- Frontend (Streamlit UI) ---
streamlit==1.43.2

# --- API (FastAPI + Swagger at /docs) ---
fastapi==0.115.12
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Utilities ---
python-dotenv==1.1.0
pydantic==2.11.1
pydantic-settings==2.8.1
